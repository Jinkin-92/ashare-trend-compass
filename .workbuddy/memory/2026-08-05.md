# 2026-08-05 工作日志

## 自动化执行：每日盘后数据更新（automation-1785839442298 首次执行）

- 运行 `scripts/daily_update.py`：阶段 1 品种树 6436 条 ✅（12:27→12:30）；阶段 2 日线增量 ✅（概念 180 条，指数/行业/个股 0 条——当日盘前无 8/5 数据属正常）；阶段 3 指标计算 ⚠️ **首次因 `database is locked` 失败**
- **锁库根因**：存在并发 `run_indicator_update()` 进程（PID 60256，12:37 启动、12:47 退出）+ 常驻 `serve_web.py`（PID 157336）。等待并发进程结束后，指标已完整写入（daily_indicator 最新 2026-08-04，6050 条）
- **补跑闭环**：`scripts/export_static.py` 成功（36.4MB，6078 品种指标、13 片价格分片、31 L1、123 L2，trade_date=2026-08-04）；`verify.py` 全部 OK 退出码 0
- **环境要点**：默认 `python`（3.13.14）无依赖，必须用 `C:\Users\Jinkin\AppData\Local\Programs\Python\Python311\python.exe` 全路径执行
- **非致命告警**：同花顺概念源约 20 个被代理掐断；tinyshare 批量补缺类型比较报错后转 baostock 兜底成功
- 临时文件 tmp_check_db.py 已清理

## 温度算法校准（用户截图对比 2026-08-04）

- 用户提供趋势动物 app 2026/08/04 截图，53 个品种
- 校准前：温度一致率 16.3%，±1档率 46.9%，系统性偏冷 (signed_mean=-1.49)
- 根因：方向分用 MA20/MA60，对短期反弹极度迟钝；阈值偏高
- 通过 `scripts/optimize_params.py` 网格搜索最优参数组合
- **核心改动**：方向 MA→MA5/MA10，ROC 增加(5,10)窗口，dir权重20→12，roc权重0.7→1.0，CONFIRM_DAYS 2→1，阈值整体下移
- 校准后：温度一致率 **49.0%**，±1档率 **87.8%**，signed_mean **+0.08**
- RS 未调整（均值+6.7，MAE 29.2）
- 校准记录：`docs/calibration/2026-08-05.md`

## 自动化执行：17:00 定时任务（同日第二次，盘后数据已出）

- 运行 `scripts/daily_update.py`，总耗时 1419.7s（约 23.7 分钟），三个阶段全部成功，无锁库
- 阶段 1 品种树 6084 条（东财全市场列表被代理掐断，降级新浪，已知现象）；阶段 2 日线增量：概念 100 条 + 个股 7882 条（tinyshare 批量补缺 5532 + baostock 兜底 357）；阶段 3 指标重算 6079 品种（温度 99158 / RS 66007 / 右侧 99195）+ 导出 35.4MB，trade_date=2026-08-05
- **当日数据已到位**：daily_price / daily_indicator 最新交易日 2026-08-05，指标覆盖 5611 品种（盘前那次执行只有 8/4 数据）
- `verify.py` 端到端验证全部 OK，退出码 0
- 告警仅两类（均非致命）：同花顺概念源被代理掐断（SSLError，兜底成功）；baostock 拉取时 WinError 10054 连接重置打印（自动重试后 357 只全部补齐）
- 经验：盘后定时任务从 16:55 启动到 17:19 完成，耗时主要在两处——概念同步（约 10 分钟，375 个概念受限于 akshare 限速）+ 个股批量补缺（约 3 分钟）
