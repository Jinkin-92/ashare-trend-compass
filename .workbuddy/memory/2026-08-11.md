# 2026-08-11 工作日志

## 投资工作台上线部署

- 交付物：`web/invest-workbench.html`（单文件，详见 2026-08-08 日志）。
- **当前线上版本**：https://a97d0e7ba601468da054ed4aab3c98e7.bj7.agentos-app.net
  - 旧链接 https://fcd0a736fb82433ab28f082332c8cda0.sh4.agentos-app.net 已 unpublish 下线。
- 部署踩坑记录（下次部署直接抄）：
  - workbuddy_cloudstudio_deploy 不带 `port: 3000` 会偶发 400/504（已实测两次失败），必须显式带 `port: 3000` 参数。
  - 部署目录放在工作区路径下，结构：`index.html` + 可选 `data/index-l1.json`。
  - 每次重新部署会生成新 sandboxId，新旧版本同时在线一段时间，需要主动 `action: "unpublish"` + `shareLink` 关掉旧版。
- 部署目录 = index.html（工作台副本）+ data/index-l1.json（当日罗盘真实数据），手机上「同步当日项目数据」按钮可用。罗盘数据停在上次管道导出日（2026-08-07），非实时。
- 已 curl 验证：首页 66.5KB、data 端点 14.1KB、源码含「红利ETF汇」与「band-chip」新类名。
- **第二轮迭代（实盘化 + UI 调整）**：
  - 演示持仓换成用户实盘截图数据：512890 红利ETF汇 91800×1.116、601899 紫金矿业 300×34.39、002714 牧原股份 100×40.20、300308 中际旭创 100×887.98。总市值 20.56万、浮盈 3630.78，与截图一致。
  - 罗盘行的「温度」列由数值（如 -33.4）改为直接显示档位文字（凉/热/平/寒），RS 列保留（1-99 趋势强度），去掉了原重复的「档位」列。行布局从 5 列收为 4 列。
  - 用户偏好固化：温度档位直接文字化、RS 单独表示趋势强度，记住这点后续别再退回数值列。
  - 日报 todos 改用真实持仓名（牧原止损复盘、紫金跌破 30 元评估），让"今天要处理"区里红标 + 顺延待办能与实盘对上号。
- 管理已发布应用：设置 - 数据管理 - 我发布的应用。
- 本地临时部署目录已清理；源文件只有 web/invest-workbench.html。
- 隐私注意：线上页面是公网可访问，演示持仓/日报均为占位数据；用户录入真实持仓时数据只在自己浏览器 localStorage，不在服务器。

## 自动化盘后更新（17:00 定时）

- 管道成功，trade_date=2026-08-11，总耗时 1226.7s。指数/行业停在 08-10（申万发布滞后），概念/个股到 08-11。
- 日报：市场宽度 2%、目标仓位 0%、无持仓；仅关注信号「医疗服务 温→热 但宽度不足暂不买入」。
- 全市场温度（5549 条）：平 27.8% / 凉 27.2% / 寒 31.2% / 冻 9.4% / 温 3.5% / 热 0.8%，市场略回暖仍弱。

## 综合校准（用户提供 2026-08-10 趋势动物校准图）

- 趋势动物 08-10 起换**新版 51 品种分类**（10 组，大量跨申万聚合口径），温度语义也变了：旧图默认档「温」，新图默认档「平」（48/51 平）。转录 `docs/calibration/2026-08-10-reference.csv`，`calib_compare.py` 新增 27 个别名，51/51 匹配。
- **多日回放**（新工具 `scripts/eval_multiday.py`，对 07-28/08-03/08-04/08-10 四个参考日用当前公式重算）：
  - RS 快窗 4/4 日全胜（MAE 11.0/12.8/16.2/12.0 vs IBD 23.7/22.4/29.2/29.3）→ **切换快窗口 (10/21/63/126, 0.4/0.3/0.2/0.1)**，回灌近 60 自然日（备份 `data/backups/trend_compass-2026-08-11-pre-rs-fast.db`）。复跑 calib_compare：RS MAE 29.3 → 12.0，与回放完全一致。
  - 温度 4 日 signed 均值 −1.36/−1.43/−2.57/−1.00，但旧参考日「温/沸」对应本地分中位 −64/−73，与「平」倒挂——参考语义变了，旧日期不可用于阈值拟合。**新图基线 29.4%/±1 72.5%/signed −1.00，等 08-11/08-12 图确认偏冷是否稳定再调阈值**（候选：负向拉伸 2.0→1.5 或「平」下界 −35→−50）。
- 验证了 08-07 工作区未提交的温度调参有效：vs HEAD(bb21d83) 29.4%/72.5% 对 7.8%/35.3%，保留。
- 门禁处置：check_rs 修两处（当日排名池过滤、groupby.last() 跳过 NaN 改取末行——概念价格有 NULL 占位行）；check_temperature 按标签直映射语义重写（C1 改抽样重算比对）；check_right_side 4 项失败为**既有债务**（备份库比对证明与本次无关，门禁内嵌重算与生产漂移，2024 年数据起）；check_data_freshness 行业指数滞后一天（已知环境现象）。
- pytest 57 全过（7 个温度测试按 08-07 状态机新语义改写）；verify.py OK；check_charts.mjs OK。
- 17:00 自动化已在快窗口代码下跑完 08-11 数据 + 导出，前端 JSON 已是新口径。
