# 2026-08-14

## 09:30 启动前端（手动触发，非定时）

- serve_web.py 后台常驻（task id vZo3a3），端口 8080
- 数据最新：2026-08-12（昨日盘后跑过 08-13 增量任务，但当晚 09:41 完成导出的数据 snapshot 是 08-13）
- 浏览器预览已打开 index.html + invest-workbench.html

## 09:35-10:00 主前端加热点看板功能（用户提需）

需求：在主前端增加一个看板，专门看「温→热」「热或沸」的二级行业或个股。

实现：
- **后端**（`src/exporter.py`）
  - 新增 `export_hotlist(out_dir, trade_date)` 函数
  - SQL 用 CTE 取每个品种的 `max(trade_date) ≤ cur` 作为 effective cur，再找严格 < cur 的 prev，避免 L2 行业指数滞后（08-12）被 cur=08-13 过滤掉
  - 输出 4 组：`l2_warm_to_hot` / `l2_hot_or_boil` / `stock_warm_to_hot` / `stock_hot_or_boil`
  - 每条记录：symbol_id, name, parent_name, temperature, prev_temperature, rs_score, rs_score_trend, is_right_side, right_side_days, close, pct_chg, effective_trade_date, prev_trade_date
  - 接入 `export_all`，让 `export_static.py` 自动产出 `web/data/hotlist.json`
- **前端**（`web/hotlist.html` + `web/js/hotlist.js`）
  - 单文件 HTML + 内联 CSS，复用 `js/list-shared.js` 的 renderTempBadge / renderRs / renderPct / renderWatchStar
  - 顶部摘要条：4 个数字（温→热/热·沸 × L2/个股）
  - 4 个 section：L2 温→热 / L2 热·沸 / 个股 温→热 / 个股 热·沸
  - 个股列表 >30 条折叠（默认前 30 + 展开剩余按钮），L2 始终全展开
  - 温→热 行加 `break-row` 类（左侧橙色横条 + 浅色背景），并把「上一日」列改渲染为 "温 →热" 强调样式
- **顶部导航**（`web/index.html`）：在 header-controls 加 `🔥 热点看板` 链接
- **调试发现**：
  1. L2 行业 symbols.data_status='no_data' 会过滤掉（如 SW_801156 医疗服务）—— 这些其实是历史数据缺失，但当前 daily_price/daily_indicator 都正常；看板上把这条 WHERE 过滤去掉了
  2. industry_l2 全表 131 个里有 8 个被标 no_data（数据不全），123 个正常

验证：
- export_static.py 跑通，hotlist.json 76KB
- 2026-08-13 数据：L2 温→热=0 / L2 热·沸=1（医疗服务 SW_801156）/ 个股 温→热=18 / 个股 热·沸=186
- verify.py 退出码 0，无 FAIL
- node --check js 语法 OK
- HTTP curl 200 全通

未跑：浏览器视觉验证（用户浏览器打开 hotlist.html 自行确认）

回滚：
- 删 web/hotlist.html + web/js/hotlist.js
- src/exporter.py 撤掉 export_hotlist 函数 + export_all 里的调用
- index.html header-controls 撤回 `🔥 热点看板` 链接

## 10:30 08-13 综合校准（8 日回放 + 收敛判断）

用户要求：把 08-13 趋势动物参考图加入综合校准，汇报收敛情况。如果没收敛则不定方案。

实现：
- 转录图 → `docs/calibration/2026-08-13-reference.csv`（51 品种：热×1 医疗服务 + 温×2 中药/化学制药 + 平×48）
- `scripts/eval_multiday.py` DEFAULT_REFS 加 08-13

环境问题（L2 行业指数发布滞后）：
- daily_update 17:10 跑管道时 `industries: 0`，L2 daily_price 仍停 08-12
- 第一次 eval_multiday 跑出来 08-13 = 88.2% 一致率，实际是「用截至 08-12 数据预测 08-13」（不真实）
- 处理：手动 `DailyPriceSync.sync_industries(end=08-13)` 补拉 155 条行业指数（tinyshare 兜底，akshare 源当日未发布）+ `recalc_temperature.py --days 7` 回灌 29727 行 → L2 daily_indicator 推进到 08-13
- 真实 calib_compare 08-13：一致 46/51 = **90.2%**、±1档 **100%**、偏差 {0:46, -1:4, +1:1}（5 个 ±1 档全无 ±2 以上）

8 日综合回放：
| 日期 | 一致 | ±1档 | signed |
|---|---|---|---|
| 07-09 | 30.8% | 70.8% | +0.29 |
| 07-21 | 30.0% | 88.6% | -0.26 |
| 07-28 | 27.1% | 75.7% | -0.51 |
| 08-03 | 37.7% | 70.5% | -0.67 |
| 08-04 | 12.2% | 57.1% | -1.67 |
| 08-10 | 86.3% | 98.0% | -0.16 |
| 08-12 | 90.2% | 98.0% | -0.04 |
| 08-13 | 90.2% | 100.0% | -0.06 |
| **pooled** | **48.1%** | **81.8%** | **-0.37** |

RS-快窗 MAE 14.8 < IBD 25.4（RS 结论稳固，不动）。

**收敛判断：阈值已稳定收敛，不再校准。**
- 新 3 日（08-10/08-12/08-13）一致率稳定在 86-90%，±1档 98-100%，signed 几乎 0
- 旧 5 日偏差是温度语义不同造成的不可同日比较，非阈值问题
- pooled 8 日（48.1%/81.8%/-0.37）vs 08-12 校准时 7 日（42.9%/79.9%/-0.41）只略好，纯属新增一日把平均拉高，阈值本身未变
- 阈值保持 `[75, 50, 30, -65, -80, -95]`（SCORE_THRESHOLDS），不动

遗留结构性问题（已超出阈值可解）：
1. 医疗服务顶层响应偏快：连续 08-12（温→沸）+ 08-13（热→沸）两次超调 1 档
2. 中药、化学制药顶部响应慢：score -3 到 +12 范围判平，参考温
3. 光伏风电、轨交军工小幅下跌被判凉（参考平）—— 对下跌品种打分偏冷

详细见 `docs/calibration/2026-08-13.md`

## 17:00 定时执行（每日盘后自动化）

- **命令**：`daily_update.py`（全路径）→ `daily_report.py --no-update` → verify.py
- **结果**：三阶段成功，总耗时 1424.3s（约 23.7 分钟，无锁库，仅 serve_web.py 常驻）
  - 阶段 2：概念 424 条 + 个股 7893 条（tinyshare 5540 + baostock 兜底 349）；`indices: 7, industries: 0`（行业指数当日数据未发布，已知滞后）
  - 阶段 3：指标 6085 品种（温度 99638 / RS 65763 / 右侧 99638）+ 导出 trade_date=**2026-08-14**，12 片价格分片，35.4MB
- **数据状态**：08-14 指标仅 stock 5502 + concept 21 条（**概念同步大量失败**：同花顺源被代理掐断，仅 21/375 成功，昨日 359 条，属已知环境现象，次日补缺自愈）；index/industry_l1/industry_l2 停在 08-13（发布滞后）
- **日报（08-14）**：市场宽度 2%（基于 08-13 L2：医疗服务 沸 + 白色家电/煤炭开采 温），目标仓位 0%，持仓 0，无操作信号
  - 信号解释：医疗服务 08-12 热 → 08-13 沸（热→沸不触发 BUY，BUY 只认温→热且宽度 2%<20%）
- **全市场温度分布（08-14，5523 条，个股+概念口径）**：沸 1.4% / 热 2.3% / 温 3.7% / 平 79.1% / 凉 10.6% / 寒 2.4% / 冻 0.4%（方案 C + 阈值重拟合后的新基线，平档占绝对多数属预期）
- **右侧状态品种** 226 个（个股口径，最长 284 天：松发股份）
- **验证**：verify.py 退出码 0 全部 OK
- portfolio.json 仍不存在（持仓管理在投资工作台 localStorage，日报显示持仓 0）