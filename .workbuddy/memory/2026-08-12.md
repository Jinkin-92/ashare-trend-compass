# 2026-08-12 工作日志

## 温度结构性修复（方案 C）—— 用户质疑偏冷后的核查与落地

- **用户问题**：L1 大量平/凉/寒、一个热都没有，但医疗服务最近很强，应该是沸/热。
- **核查**：数据无误（医疗服务 20 日 +24.5%、创 250 日新高）；当前公式给「热」，参考「沸」，低一档。
  根因 = 回撤因子（20% 权重）创新高时贡献恒为 0（纯拖累项，raw 上限被压到 0.8）+ 负向拉伸 ×2.0 放大冷侧。
- **变体实测**（08-10 参考 + 3 历史参考日回放）：C 方案（dd→区间位置 + 负向拉伸 1.5）最优：
  一致率 29.4→41.2%、±1档 72.5→86.3%、signed −1.00→−0.71；医疗服务 热→沸（与趋势动物一致）。
- **已落地**（用户四选一拍板 C）：
  - `src/indicators/temperature.py`：dd 因子改 250 日区间位置 `tanh((pos−0.5)×4)`；负向拉伸 2.0→1.5；
    模块文档串/SCORE_THRESHOLDS 陈旧值同步修正。
  - `scripts/recalc_temperature.py --days 60` 全品种温度回灌（备份 `data/backups/trend_compass-2026-08-12-pre-temp-rangepos.db`）。
  - 右侧状态窗口内行靠次日 daily_update 的 21 日重写窗口 + 种子回滚自愈（未单独回灌）。
- **影响提示**：温度分布整体转暖 → 日报市场宽度/目标仓位信号次日起变化（08-12 前市场宽度 2%、目标仓位 0% 的极端读数会明显缓解）。
- 门禁复验：pytest 17/17（test_temperature）过；eval_multiday 重跑确认上表数字；
  check_temperature（重算比对型）/check_rs 待回灌完成后复跑。
- 插曲：eval_multiday.py 的 IBD 对照列原来引用模块默认值，RS 切换后两列口径相同，已改为写死 IBD (21/63/126/252)。
- 时间锚点：08-12 17:00 自动化已正常跑完（daily_price/indicator 到 08-12；RS 已是快窗；温度为 C 前公式，被本次回灌覆盖）。

## 回灌与复验闭环（08-13 晨完成）

- 首次回灌在第 9 分块卡死被超时杀掉（原因未明，疑似瞬时锁库；已确认无残留进程、写锁空闲）。
  重跑加逐分块日志 + 禁超时，顺利完成：**13/13 分块，251,778 行（2026-06-13 ~ 08-12）**。
- 复验全绿：calib_compare（08-10 参考）温度 41.2%/±1档 86.3%、RS MAE 12.0（与回放一致）；
  check_temperature PASS（444 品种重算比对）、check_rs PASS（5531 品种）、pytest 57/57、
  export_static（trade_date=2026-08-12, 35.3MB）+ verify.py OK。
- 教训：长任务必须 disable_timeout；重跑前务必确认无残留 python 进程 + 写锁空闲。
