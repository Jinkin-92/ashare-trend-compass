# 2026-08-13 日志

## 温度分档阈值重拟合（7 日综合拟合）

用户给 2026-08-12 趋势动物校准图（新版 51 品种），明确要求不做单日拟合、用全部历史校准图综合拟合。

- 转录 `docs/calibration/2026-08-12-reference.csv`（温×2：医疗服务、化学制药；其余 49 个全平）。
- 过程中发现三个环境问题并处理：
  1. 行业指数行情 08-12 漏抓（行业源滞后一天）→ 手动 `DailyPriceSync.sync_industries` 补 155 行；
  2. `recalc_rs.py` inner join 温度列，行业 08-12 无指标行导致 RS 丢失 → 由 17:10 的 daily_update 阶段 3 自愈（不要kill 正在跑的 daily_update，它持 DB 写锁）；
  3. **D 盘 100% 满**（82G 用满）→ 改前备份放 `F:\trend-compass-backups\trend_compass-2026-08-13-pre-threshold-refit.db`。data/backups/ 里还有两个 ~490MB 旧备份，空间紧张时可迁移到 F 盘。
- 分析：`eval_multiday.py` DEFAULT_REFS 扩到全部 7 个参考日（417 样本）。温度 signed 偏差 7/7 日全负（-2.22 ~ -0.31），系统性偏冷且跨时代同向；RS 快窗 7/7 日优于 IBD（pooled MAE 13.5 vs 24.7），不再动。
- 变更（一次一个变量）：`SCORE_THRESHOLDS` = [75, 50, 30, -65, -80, -95]（温 20→30、平/凉 -35→-65、凉/寒 -60→-80、寒/冻 -85→-95），`_raw_bucket_idx` 消除硬编码改读常量；测试边界用例同步更新。
- 效果：回放口径全 7 日一致 22.8%→42.9%、±1档 61.2%→79.9%、signed −0.99→−0.41；新两日 45.1%→88.2%。回灌 60 日 257,751 行后库存值口径 08-10 86.3%/98.0%、08-12 90.2%/98.0%。
- 验证：pytest 57/57、check_temperature PASS、check_rs PASS、verify.py 19 项 OK、export_static 到 08-13。既有 FAIL 不变（check_right_side 4 项漂移、check_data_freshness 行业源滞后）。

## 遗留观察点

- 医疗服务 08-12 参考降为「温」，我们 score 82.5 仍判「沸」：顶部响应偏慢，样本仅 1 例，暂不动，继续观察。
- 参考「温」与「平」在我们 score -22~+26 区间不可分（化学制药 vs 煤炭），模型结构性差异，阈值无解。
- 文档：`docs/calibration/2026-08-12.md`（当日报告）、correction-workflow.md（新基线）已更新。
