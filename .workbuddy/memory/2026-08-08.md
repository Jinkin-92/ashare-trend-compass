# 2026-08-08 工作日志

## 交付：个人投资工作台（单文件 HTML）

- 文件：`web/invest-workbench.html`（约 66KB，全内联零外链，无 emoji）。
- 四个模块：01 今天要处理（持仓浮亏 ≤-5% 触发关注、≤-8% 止损审视，日报未完成待办自动顺延、逾期标红）／02 趋势罗盘（内联演示快照 7 指数 + 31 申万一级行业，七档温度分布图、热度榜，内置「同步当日项目数据」按钮 fetch `./data/index-l1.json`，字段与导出结构一致）／03 持仓（统计条 + 增删改表单，红涨绿跌）／04 简要日报（情绪标签 + 待办勾选）。
- 存储：`wb_invest_v1`（positions/reports）、`wb_invest_v1:snap`（罗盘快照）、`wb_invest_dismiss`（当日已处理）、`wb_invest_meta`（备份提醒进度）。输入即保存；满 20 条弹备份提醒；写入失败/数据损坏均有警示条 + 恢复按钮；清空二次确认；导入不限条数（id 一律重生成防注入）。
- 视觉：暖纸底色 + 墨色 + 陶土赭石主色，衬线标题，无卡片堆砌，520px 以下单列布局 + 按钮 44px + 输入 16px + safe-area。
- 验证：node --check 语法通过；虚拟 DOM 冒烟测试 14 项渲染断言全过；损坏恢复路径测试通过；本地 http.server 实测 200。测试服务器已关闭。
- 提醒用户：页面放在 web/ 下经服务器访问时「同步」按钮可用（file:// 下 fetch 会失败，自动降级用快照）。
