# 2026-08-19

- 完成趋势罗盘温度/RS 校准、门禁和导出相关改动，并加入工作台 inline 数据构建脚本。
- 修复个人投资工作台主脚本的引号语法错误，验证工作台可渲染 7 个指数、31 个行业及信号数据。
- 提交前验证通过：`pytest` 57 passed，`check_rs.py` 全部通过，`check_temperature.py` 全部通过，`scripts/build_workbench_deploy.py` 构建成功，工作台主脚本 `node --check` 通过。
- 已创建 Git 提交 `3e667cd` 并推送到 `origin/main`。未提交的运行产物包括 `data/backups/`、`data/portfolio.json`、`data/reports/`、`tmp/` 及两份历史工作台快照。
